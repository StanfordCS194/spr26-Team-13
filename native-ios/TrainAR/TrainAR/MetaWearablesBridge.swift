import Foundation

/// Placeholder for the real Meta Wearables Device Access Toolkit integration.
///
/// Keep this file compiling without the SDK so the project can run today. When
/// the Meta side is ready, add the SDK package in Xcode and replace this with a
/// concrete `GlassesBridge` implementation that forwards registration, session,
/// camera, and photo events as `GlassesBridgeEvent` values.
final class MetaWearablesBridge {
    func configure() {
        // TODO: Wearables.configure()
    }
}

